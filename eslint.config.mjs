import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // These React-Compiler-oriented rules fire on intentional patterns we
      // use deliberately: SSR "mounted" flags (theme toggle), resetting a
      // form's local state when a dialog opens, and reading the current time
      // to label a card as "due now". They're style/perf hints, not bugs, so
      // we keep them visible as warnings rather than blocking the build.
      "react-hooks/set-state-in-effect": "warn",
      "react-hooks/purity": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
