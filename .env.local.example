# Optional. Server-side Anthropic API key used by /api/generate when a user
# hasn't provided their own key in Settings (BYOK). Without this (and without a
# BYOK key), everything works except AI card generation.
#
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
